# Frontend build-time public config (VITE_-prefixed = shipped to the browser).
# NOTE: the MapTiler tile key is no longer a build var — it's served at runtime by the backend
# via GET /api/config (set MAPTILER_TILE_KEY on the backend Worker). Nothing required here for maps.

# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
out/
.nuxt/
.output/
.vite/

# Environment & secrets
.env
.env.local
.env.*.local
.env.production

# Personal/private scratch (holds real credentials — never commit)
.private
.private/

# Cloudflare / Wrangler secrets & build
.dev.vars
.dev.vars.*
!.dev.vars.example
.wrangler/

# Monorepo task cache
.turbo/

# Claude Code — may contain auth tokens / credentials
.claude/settings.local.json
# Local-scope MCP config (may hold tokens) — keep committed .mcp.json secret-free
.mcp.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Package manager locks (keep only one — remove whichever you don't use)
# yarn.lock
# pnpm-lock.yaml

# Real community data (PII) — never commit; used locally by tools/seed-import
data/
